name: Automatic Deployment

on:
  push:
    branches: [staging]

jobs:
  deployment:
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - name: Welcome
        run: echo Start deploying Sen Suite
      - name: Deploy static to Render
        run: curl ${{ secrets.RENDER_DEPLOY_HOOK }}
