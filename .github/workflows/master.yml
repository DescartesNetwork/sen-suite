name: Automatic Deployment

on:
  push:
    branches: [master]

jobs:
  deployment:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Welcome
        run: echo Start deploying Sen Suite to Production
      - name: Deploy static to Render
        run: curl ${{ secrets.RENDER_DEPLOY_HOOK }}
